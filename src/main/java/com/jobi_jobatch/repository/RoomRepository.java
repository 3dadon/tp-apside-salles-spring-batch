package com.jobi_jobatch.repository;

import com.jobi_jobatch.model.Room;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoomRepository extends JpaRepository<Room, Integer> {
}
